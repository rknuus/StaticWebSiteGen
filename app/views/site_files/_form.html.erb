<%= form_for(@site_file) do |f| %>
  <% if @site_file.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@site_file.errors.count, "error") %> prohibited this site_file from being saved:</h2>

      <ul>
      <% @site_file.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="field">
		<!-- FIXME: replace by file_field -->
    <%= f.label :path %><br />
    <%= f.text_field :path, :size => get_pathfield_size %>
  </div>
	<%= f.hidden_field :site_id, :value => @site_file.site_id %>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
