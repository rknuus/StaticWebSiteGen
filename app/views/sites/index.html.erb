<h1>My sites</h1>

<p id="notice"><%= notice %></p>

<table>
  <tr>
    <th></th>
    <th></th>
    <th></th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @sites.each do |site| %>
  <tr>
    <td><%= site.name %></td>
    <td><%= edit_link(site) %></td>
    <td><%= link_to 'Edit globals', site_path(site), method: :get %></td>
    <td><%= pages_link(site.id) %></td>
    <td><%= generate_link(site) %></td>
    <td><%= link_to 'Destroy', site, confirm: 'Are you sure?', method: :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New site', new_site_path %>
