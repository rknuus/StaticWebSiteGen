<h1>Listing site_texts</h1>

<table>
  <tr>
    <th>Site</th>
    <th>Content</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @site_texts.each do |site_text| %>
  <tr>
    <td><%= site_text.site_id %></td>
    <td><%= site_text.content %></td>
    <td><%= link_to 'Show', site_text %></td>
    <td><%= link_to 'Edit', edit_site_text_path(site_text) %></td>
    <td><%= link_to 'Destroy', site_text, confirm: 'Are you sure?', method: :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Site text', new_site_text_path %>
