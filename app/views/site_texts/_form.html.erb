<%= form_for(@site_text) do |f| %>
  <% if @site_text.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@site_text.errors.count, "error") %> prohibited this site_text from being saved:</h2>

      <ul>
      <% @site_text.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :site_id %><br />
    <%= f.number_field :site_id %>
  </div>
  <div class="field">
    <%= f.label :content %><br />
    <%= f.text_area :content %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
